{
  "name": "@palm-tree-css/frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf .cache dist build",
    "build": "parcel build src/views/*.html --public-url .",
    "prepublish": "npm run build",
    "publish": "gh-pages -d dist",
    "start": "parcel src/views/*.html"
  },
  "devDependencies": {
    "parcel-bundler": "1.12.5"
  }
}
